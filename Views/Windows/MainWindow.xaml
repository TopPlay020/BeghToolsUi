<Window
    x:Class="BeghToolsUi.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BeghToolsUi.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:windows="clr-namespace:BeghToolsUi.ViewModel.Windows"
    xmlns:xf="clr-namespace:XamlFlair;assembly=XamlFlair.WPF"
    xmlns:xfc="clr-namespace:XamlFlair.Controls;assembly=XamlFlair.WPF"
    Title="BeghToolsUi"
    Width="1200"
    Height="600"
    d:DataContext="{d:DesignInstance Type=windows:MainWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <DataTemplate x:Key="MenuItemTemplate">
            <Grid Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    Width="24"
                    Height="24"
                    Margin="4"
                    VerticalAlignment="Center"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Source="{Binding Icon}" />
                <TextBlock
                    Grid.Column="1"
                    Margin="8,0"
                    VerticalAlignment="Center"
                    Text="{Binding Title}"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
        </DataTemplate>
        <Style x:Key="MenuListViewStyle" TargetType="xfc:AnimatedListView">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="xf:Animations.AnimateOnLoad" Value="True" />
            <Setter Property="xf:Animations.Items" Value="{xf:Animate BasedOn={StaticResource FadeIn}}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="ItemTemplate" Value="{StaticResource MenuItemTemplate}" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border
            Grid.Column="0"
            Margin="10,0,0,10"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image
                        Grid.Column="0"
                        Height="{Binding ElementName=AppNameViewBox, Path=ActualHeight}"
                        Margin="0,0,5,0"
                        Source="/Assets/Icons/App.png"
                        Stretch="Uniform" />
                    <Viewbox x:Name="AppNameViewBox" Grid.Column="1">
                        <TextBlock FontWeight="Bold" Text="eghToolsUi" />
                    </Viewbox>
                </Grid>
                <Border Grid.Row="1" Padding="5,0,5,5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Main items  -->
                        <xfc:AnimatedListView
                            x:Name="MainMenuListView"
                            Grid.Row="0"
                            ItemsSource="{Binding MainMenuItems}"
                            SelectedItem="{Binding MainSelectedItem, Mode=TwoWay}"
                            Style="{StaticResource MenuListViewStyle}" />

                        <!--  Footer items  -->
                        <xfc:AnimatedListView
                            x:Name="FooterMenuListView"
                            Grid.Row="1"
                            ItemsSource="{Binding FooterMenuItems}"
                            SelectedItem="{Binding FooterSelectedItem, Mode=TwoWay}"
                            Style="{StaticResource MenuListViewStyle}" />
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border
            Grid.Column="1"
            Margin="10,0,10,10"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="10">
            <ContentControl
                x:Name="MainView"
                xf:Animations.Primary="{xf:Animate BasedOn={StaticResource FadeInAndSlideFromTop},
                                                   Event=None}"
                xf:Animations.PrimaryBinding="{Binding IsAnimated}"
                Content="{Binding MainView}" />
        </Border>
    </Grid>
</Window>
